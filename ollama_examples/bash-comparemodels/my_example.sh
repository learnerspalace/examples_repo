#! /usr/bin/env bash

OPTIONS=("A", "B", "C", "D")
select OPTION in $OPTIONS; do
    echo "--------------------------------------------------------------"
    echo "Loading the model $OPTION into memory"
    ((gyfhyg++))
    echo "count=$gyfhyg"
done